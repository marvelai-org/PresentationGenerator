# Supabase Configuration 
# Copy this file to .env.local and replace the example values with your real credentials

# Your Supabase project URL (required for real Supabase client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (required for real Supabase client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Set to 'true' to enable detailed database operation logging
# NEXT_PUBLIC_MOCK_DEBUG=true

# Note: The application will use the mock database implementation if:
# 1. These variables are missing
# 2. Running in a CI environment
# 3. An error occurs when trying to connect to Supabase 